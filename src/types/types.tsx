export interface IBody {
    ddelete: JSX.Element
    insert: JSX.Element
    edit: JSX.Element
}

export interface IRow {
    id: number
    firstName: string
    lastName: string
    phone: number
    registrationDate: string
}
